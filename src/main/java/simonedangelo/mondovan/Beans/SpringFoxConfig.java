package simonedangelo.mondovan.Beans;

/*@Configuration
@EnableSwagger2*/
public class SpringFoxConfig {
/*    @Bean
    public Docket swaggerConfig() {
        Docket d = new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.any())
                .paths(PathSelectors.any())
                .build();
        return d;
    }

    private ApiInfo apiCustomData() {
        return new ApiInfo(
                "VanWorld API Application",
                "VanWorld API Documentations",
                "1.0",
                "VanWorld API Term Services",
                new Contact("Simone D'Angelo", "http://localhost:8080", "simone.dangelo636@gmail.com"),
                "VanWorld License",
                "http://localhost:8080", Collections.emptyList()
        );
    }*/
}
